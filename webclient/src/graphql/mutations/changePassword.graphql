mutation ChangePassword($token: String!, $newPassword: String!, $retypedPassword: String!) {
  changePassword(token: $token, newPassword: $newPassword, retypedPassword: $retypedPassword) {
    ...CommonUserResponse
  }
}